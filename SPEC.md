# AMONTY · Train With The Brain — SPEC / תכנית פיתוח

> מסמך חי. גרסה 0.3 — עודכן לפי סקירה ניטרלית (מפתח BYO כברירת מחדל, בדיקת-עשן, הערות עלות). כל סעיף פתוח לשינוי.

## 0. מטרות-על (מה שמנחה הכל)

1. **להעלות את המוצר לאוויר** ולתחזק אותו בגרסאות.
2. **CI/CD מסודר מול git** — כל merge לענף הראשי מעלה גרסה אוטומטית.
3. **אפס תקציב / אפס עלויות** — אירוח חינמי, מפתחות עם תקרת הוצאה, שימוש יעיל בטוקנים.
4. **LLM רק כשקריטי** — רוב יצירת התכנית עוברת ל**קוד דטרמיניסטי**, לא ל-LLM. מודל הכי זול שמתאים למשימה.
5. **חוויה למאמן לא-טכני** — טפסים הופכים ל-wizards; דף בית עם "תכניות שמורות" או כפתור "הפק תכנית חדשה".

## 1. שיטת עבודה — בנאי + סוקר ניטרלי

המשתמש אינו טכנולוג ולא נדרש לאמת קוד. לכן:

- **Claude (בנאי):** מתכנן, כותב קוד, מעלה גרסאות.
- **Subagent ניטרלי (סוקר/מתווך):** סוקר כל הצעה/PR משמעותי, נותן חוות דעת עצמאית (נכונות, עלות, סיכון, פשטות), ומסכם בעברית פשוטה למשתמש. משמש בקרה בלי להעמיס על המשתמש.
- שינויים גדולים (ארכיטקטורה, עלויות) עוברים סקירה לפני מיזוג.

## 2. מצב קיים (נקודת הפתיחה)

מחולל מערכי אימון מבוסס AI (עברית, RTL). Next.js 14 + React 18.
כל האפליקציה בקובץ אחד: `components/TrainApp.tsx` (~3,358 שורות) — HTML כמחרוזת + JS כמחרוזת מוזרק ב-runtime. אין backend; נתונים ב-localStorage; קריאות AI ישירות מהדפדפן עם מפתח המשתמש.

**חוב טכני מרכזי:** ~1,130 שורות קוד **מת וכפול** בתוך `BODY_HTML` (script שלא רץ). מונוליט אחד. אין בדיקות/lint/CI.

## 3. ארכיטקטורת עלות (הלב של הכיוון החדש)

| רכיב | החלטה | למה |
|---|---|---|
| אירוח | Vercel Free / Hobby (Next.js native) | חינמי, deploy אוטומטי מ-git. **הערה:** Hobby = שימוש לא-מסחרי; אם הופך לעסק → Pro ~$20/חודש |
| יצירת תכנית | **קוד תחילה** — ספריית תרגילים + מנוע חוקים שמרכיב מבנה (חימום/עיקרי/משחק/סיום), בוחר תרגילים לפי ענף/נושא/ציוד/גיל | 0 עלות טוקנים לרוב התכניות |
| LLM | רק לחלקים קריטיים: "תפתיע אותי" בין-ענפי, פיתוח בסיס של מאמן, יועץ, ניתוח PDF/תמונה | ממזער קריאות |
| מודל | הזול שמתאים (Haiku 4.5) כברירת מחדל; שדרוג רק כשצריך | עלות מינימלית |
| מפתח API | **ברירת מחדל: מפתח-המאמן** (BYO-key) = **אפס עלות לבעל המוצר**. מפתח-פרויקט מרכזי בצד-שרת — רק בעתיד, **חובה עם הגנה** (rate-limit, gate, תקרת max_tokens, cap ב-Console) | לא להפוך לברז טוקנים ציבורי; capped-cost ≠ zero-cost |
| התמדה | localStorage תחילה; ענן חינמי (Supabase free) רק אם/כשצריך סנכרון | דוחה עלות עד שיש צורך |

**עיקרון:** אם אפשר לייצר תוצר טוב בקוד — לא קוראים ל-LLM.

## 4. עקרונות פיתוח

1. כל שינוי ניתן לאימות (build עובר + האפליקציה רצה) לפני התקדמות.
2. אין רגרסיה בפיצ'ר קיים בלי חלופה.
3. שלבים קטנים, ניתנים למיזוג ולהעלאה.
4. הסוקר הניטרלי מאשר שינויים גדולים.

---

## שלב 0 — עלייה לאוויר + CI/CD (קודם כל!)

מטרה: מוצר חי עם צינור העלאת גרסאות, לפני ריפקטור.

- [ ] 0.1 חיבור הריפו ל-Vercel (Free) — deploy אוטומטי מהענף הראשי.
- [ ] 0.2 CI ב-GitHub Actions: `next build` + lint על כל PR (חוסם merge אם נכשל).
- [ ] 0.3 `.eslintrc` + `prettier` + tsconfig סביר.
- [ ] 0.4 preview deployments ל-PRs (Vercel נותן חינם) — לראות שינוי לפני מיזוג.
- [ ] 0.5 **בדיקת-עשן Playwright** (חינמי ב-CI): טוען את האפליקציה, לוחץ על כל 5 הלשוניות, מוודא שאלמנטים מרכזיים מוצגים. **הכרחי לפני שלב 1 ושלב 4** — כי "build עובר" לא מאמת קוד שיושב בתוך מחרוזות.

**הצלחה:** push לענף הראשי → גרסה עולה אוטומטית; PR מריץ build+lint+preview+smoke.

---

## שלב 1 — מחיקת הקוד המת (Quick win, סיכון אפס)

- [ ] 1.1 לאמת שהבלוק בתוך `BODY_HTML` (script ~שורה 376, ~1,130 שורות) מת.
- [ ] 1.2 למחוק אותו; להשאיר HTML אמיתי + `MAIN_JS`.
- [ ] 1.3 לוודא שכל 5 הלשוניות עובדות.

**הצלחה:** התנהגות זהה, ~1,100 שורות פחות, אין הגדרה כפולה.

---

## שלב 2 — מנוע יצירת תכנית בקוד (חיסכון העלות המרכזי)

מטרה: להוריד את רוב יצירת התכנית מ-LLM לקוד.

- [ ] 2.1 להגדיר סכמת נתונים לתרגיל (שם, ענף, נושא, ציוד, גיל, דגשים, ארגון).
- [ ] 2.2 לבנות ספריית תרגילים ראשונית (seed) — עברית, לפי ענפים נפוצים.
- [ ] 2.3 מנוע הרכבה: חלוקת זמן לחלקים + בחירת תרגילים לפי קלט המשתמש, דטרמיניסטי.
- [ ] 2.4 להשאיר "hook" ל-LLM רק לחלקים קריטיים (בין-ענפי יצירתי, בסיס מאמן, ליטוש).
- [ ] 2.5 fallback: אם אין כיסוי בקוד לענף/נושא → קריאת LLM אחת (Haiku).

**הצלחה:** תכנית טיפוסית נוצרת ב-0 קריאות LLM; איכות נבדקת מול הפלט הישן.

---

## שלב 3 — Wizards + דף בית

- [ ] 3.1 להפוך את טופס הבנייה ל-wizard רב-שלבי (ענף → פרטים → נושא → ציוד → סקירה).
- [ ] 3.2 דף בית: "תכניות שמורות" (אם יש) או כפתור בולט "הפק תכנית חדשה".
- [ ] 3.3 שמירה על שאר הלשוניות (יועץ, ידע, העברה) בזמן המעבר.

**הצלחה:** מאמן חדש מגיע לתכנית ב-flow ברור, בלי מסך עמוס.

---

## שלב 4 — פירוק המונוליט לקומפוננטות

(אחרי שהמוצר חי ויציב.)

- [ ] 4.1 CSS למודול נפרד. 4.2 שכבת אחסון אחת (`lib/storage.ts`).
- [ ] 4.3 שכבת AI (`lib/ai.ts`). 4.4 פירוק ל-React components לפי לשונית.
- [ ] 4.5 החלפת DOM ידני ב-React state בהדרגה.

**הצלחה:** אין `dangerouslySetInnerHTML`/הזרקת script; הקובץ המרכזי < 200 שורות.

---

## שלב 5 — התמדה בענן (רק אם צריך)

- [ ] Supabase Free (Auth + DB) לסנכרון בין מכשירים — נפתח רק אם עולה צורך אמיתי.

## שלב 6 — העמקת פיצ'רים (לפי תעדוף)

הדמיה טקטית, שיתוף מערך בין מאמנים, ספריית תרגילים משותפת, יועץ עם הקשר מלא, ניתוח וידאו.

---

## 5. סדר עבודה

0 (לאוויר+CI/CD) → 1 (קוד מת) → 2 (מנוע קוד) → 3 (wizards) → 4 (פירוק) → 5–6 לפי צורך.

## 6. סיכונים

- **תוכן ספריית התרגילים הוא סיכון המוצר האמיתי** (מאמץ תוכן, לא רק קוד). מנוע טוב בלי תרגילים איכותיים בעברית = חסר ערך. מיטיגציה: seed איכותי לענפים הנפוצים תחילה; fallback ל-LLM רק לפערים (מודע לכך שזה מחזיר עלות).
- **מפתח מרכזי בצד-שרת = חור אבטחה** אם בלי הגנה — לא לפרוס בלי rate-limit + gate + תקרת max_tokens.
- **"build עובר" ≠ התנהגות זהה** (קוד במחרוזות) — מיטיגציה: בדיקת-עשן Playwright (שלב 0.5) לפני מחיקה/פירוק.
- פירוק המונוליט עלול לשבור התנהגות דקה — מיטיגציה: תתי-שלבים + סקירה ניטרלית.

## 7. שאלות פתוחות (לא חוסמות)

- חשבון Vercel + Anthropic (עם תקרת הוצאה) — בבעלות המשתמש; להגדיר בכניסה לשלב 0.
- היקף ספריית התרגילים ההתחלתית (כמה ענפים/תרגילים ל-MVP).
