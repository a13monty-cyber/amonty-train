# AMONTY · Train With The Brain — SPEC / תכנית פיתוח

> מסמך חי. גרסה 0.1 — טיוטה לדיון. כל סעיף פתוח לשינוי.

## 1. מצב קיים (נקודת הפתיחה)

מחולל מערכי אימון מבוסס AI למאמני ספורט (עברית, RTL). Next.js 14 + React 18.
כל האפליקציה בקובץ אחד: `components/TrainApp.tsx` (~3,358 שורות) — HTML כמחרוזת + JS כמחרוזת שמוזרק ב-runtime. אין backend; נתונים ב-localStorage; קריאות AI ישירות מהדפדפן עם מפתח המשתמש.

**5 לשוניות:** בנייה · מערכים · יועץ · ידע · העברה.
**פיצ'רים:** בניית מערך (streaming), ציר זמן, הדמיה טקטית (canvas), ניתוח PDF, ניתוח תמונות/וידאו, יועץ AI, ייצוא Word/Excel/PDF, ייבוא/ייצוא JSON.

## 2. החלטות מכוננות

| נושא | הכרעה | סטטוס |
|---|---|---|
| ארכיטקטורה | היברידי מדורג — ניקוי + Serverless עכשיו, נתיב הגירה ל-Backend מתוכנן | מוסכם (זמני) |
| עדיפות ראשונה | ניקוי חוב טכני לפני כל פיצ'ר | מוסכם (זמני) |
| קהל | לא הוכרע — ה-SPEC שומר על שתי האופציות פתוחות | פתוח |
| מודל AI | שדרוג ל-Sonnet 5 / Opus 4.8 | לתיאום |

## 3. עקרונות

1. כל שינוי ניתן לאימות (build עובר, האפליקציה רצה) לפני התקדמות.
2. אין רגרסיה בפיצ'ר קיים בלי כיסוי חלופי.
3. שלבים קטנים וניתנים למיזוג, לא "big bang".
4. החלטות ארכיטקטורה גדולות (Backend, DB, Auth) — לא ננעלות עד שלב 3.

---

## שלב 0 — ייצוב ותשתית (בסיס בטוח)

מטרה: להפוך את הריפו לכזה שאפשר לפתח בו בלי לשבור.

- [ ] 0.1 להוסיף `.eslintrc` + `prettier` + `tsconfig` מחמיר יותר (הדרגתי).
- [ ] 0.2 להוסיף `SessionStart` hook / סקריפט שמריץ `next build` כ-smoke test.
- [ ] 0.3 להריץ build נקי ולתעד אזהרות קיימות.
- [ ] 0.4 להוסיף CI בסיסי (build + lint על PR).

**קריטריון הצלחה:** `next build` עובר, lint רץ, CI ירוק על PR ריק.

---

## שלב 1 — מחיקת הקוד המת (Quick win, סיכון אפס)

הבעיה: `BODY_HTML` מכיל `<script>` (שורה ~376) עם ~1,130 שורות JS שלעולם לא רצות (סקריפט ב-`dangerouslySetInnerHTML` לא מורץ). זהו עותק כפול של `MAIN_JS` שכן רץ.

- [ ] 1.1 לאמת בוודאות שהבלוק מת (הרצה מקומית + בדיקה שהמחיקה לא משנה התנהגות).
- [ ] 1.2 למחוק את הבלוק הכפול מ-`BODY_HTML`, להשאיר רק את ה-HTML האמיתי + `MAIN_JS`.
- [ ] 1.3 לוודא שכל 5 הלשוניות והזרימות עובדות (בנייה, שמירה, יועץ, הדמיה, ייצוא).

**קריטריון הצלחה:** האפליקציה מתנהגת זהה, הקובץ קטן ב-~1,100 שורות, אין הגדרה כפולה של אף פונקציה.

---

## שלב 2 — פירוק המונוליט לקומפוננטות

מטרה: מ-vanilla-בתוך-string ל-React אמיתי, מודולרי ובר-תחזוקה.

- [ ] 2.1 לחלץ CSS ל-CSS Modules / קובץ ייעודי (כרגע inline בתוך string).
- [ ] 2.2 להוציא את שכבת האחסון למודול אחד (`lib/storage.ts`) — לאחד את `localStorage` הישיר ו-`window.storage` לממשק אחד.
- [ ] 2.3 להוציא את שכבת ה-AI למודול (`lib/anthropic.ts`) — buildPlan, advisor, photo/pdf analysis.
- [ ] 2.4 לפרק ל-React components לפי לשונית: `BuildTab`, `PlansTab`, `AdvisorTab`, `KnowledgeTab`, `TransferTab`, + `CourtCanvas`, `PlanResult`, מודלים.
- [ ] 2.5 להחליף state ידני (getElementById) ב-React state בהדרגה, לשונית-לשונית.

**קריטריון הצלחה:** אין `dangerouslySetInnerHTML`, אין הזרקת script ידנית, כל פיצ'ר עובד, הקובץ המרכזי מתחת ל-200 שורות.

> הערה: שלב 2 גדול — נחלק לתתי-PRs לפי לשונית.

---

## שלב 3 — הכרעת ארכיטקטורה (Backend / התמדה / Auth)

נפתח רק אחרי בסיס יציב. שתי אופציות שה-SPEC שומר פתוחות:

**מסלול A — Backend קל:**
- Next.js API route כ-proxy ל-Anthropic (המפתח בשרת, לא בדפדפן).
- Auth (למשל Auth.js / Clerk).
- DB להתמדה (Postgres/Supabase) — מערכים, ידע, משתמשים.
- בסיס למונטיזציה (מכסות, tiers).

**מסלול B — Serverless משופר:**
- שמירה על BYO-key, אבל שיפור onboarding + גיבוי ענן אופציונלי (משתמש מביא storage).
- סנכרון דרך ייצוא/ייבוא אוטומטי / קובץ.

**החלטה נדרשת בכניסה לשלב זה:** A או B, לפי הכרעת קהל (סעיף 2).

---

## שלב 4 — שדרוג AI ואיכות תוצרים

- [ ] 4.1 שדרוג מודל ל-Sonnet 5 / Opus 4.8 (+ בדיקת איכות מערכים).
- [ ] 4.2 שיפור prompts (מבנה, עקביות, טיפול ב-edge cases).
- [ ] 4.3 טיפול טוב יותר ב-JSON parsing ובשגיאות streaming.

---

## שלב 5 — העמקת פיצ'רים (אחרי בסיס יציב)

מועמדים (לתעדוף מאוחר יותר):
- שיפור ההדמיה הטקטית (טמפלטים, אנימציה, שמירה).
- שיתוף מערך בין מאמנים (link/export).
- ספריית תרגילים משותפת.
- שיפור יועץ ה-AI (הקשר מלא של המערך).
- ניתוח וידאו מתקדם.

---

## 4. סדר עבודה מוצע

1. שלב 0 (תשתית) → 2. שלב 1 (מחיקת קוד מת) → 3. שלב 2 (פירוק, מדורג) → 4. הכרעת שלב 3 → 5. שלבים 4–5 לפי עדיפות.

## 5. סיכונים

- פירוק המונוליט עלול לשבור התנהגות דקה (canvas, RTL, print) — מיטיגציה: תתי-שלבים + אימות ידני אחרי כל אחד.
- הכרעת Backend דורשת החלטת קהל/מונטיזציה — לא לחסום את שלבים 0–2 בגללה.

## 6. שאלות פתוחות (לא חוסמות)

- קהל היעד הסופי (אישי מול קהילה)?
- מודל מונטיזציה, אם בכלל?
- העדפת ספקי Auth/DB אם הולכים על מסלול A?
